import React from 'react';

interface Props {
  // Props interface goes here
}

const Logout: React.FC<Props> = ({ /* Destructure your props here */ }) => {
  return (
    <div>Logout</div>
  );
};

export default Logout;